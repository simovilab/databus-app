---
icon: lucide/rocket
---

# Deployment

Getting a build in front of a real device — over HTTPS as an installable web
app for fast iteration, or as a true native build for the telemetry hot path.

| Page | What you will find |
| --- | --- |
| [VPS HTTPS device testing](vps-https-testing.md) | How the web build gets served over real HTTPS with no app-store involvement |
| [Native builds](native-builds.md) | Android/iOS builds and what's still open before a real native release |
