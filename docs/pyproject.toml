[project]
name = "databus-app-docs"
version = "0.1.0"
description = "Documentation for the Databús operator app (Ionic Vue + Capacitor)."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.46",
]
